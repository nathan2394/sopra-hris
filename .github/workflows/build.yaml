name: SOPRA-HRIS FE

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          node-version: "16"
      - run: npm install --force
      - run: npm run build
