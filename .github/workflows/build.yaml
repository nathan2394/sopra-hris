name: SOPRA-HRIS FE

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: 🔨 Get latest code
        uses: actions/checkout@v3
        with:
          node-version: "16"

    - name: 🔨 Build Project
        run: |
          npm init
          npm install --force
          npm run build
        env:
          CI: false
